meta {
  name: Post Address
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/addresses
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "line1": "{{$randomInt}} Main St",
    "line2": "Apt {{$randomInt}}",
    "city": "New York",
    "state": "NY",
    "zipCode": "10001"
  }
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
}
